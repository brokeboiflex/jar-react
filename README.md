# JAR React

Deprecated package source. The combined React and Preact package now lives in
[`brokeboiflex/jar`](https://github.com/brokeboiflex/jar) and publishes as
`jar`.

Use the canonical package instead:

```sh
npm install jar
```
